spring:
  config:
    activate:
      on-profile: prod

logging:
  level:
    root: WARN
    com.example.rag: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never
