<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rag.mapper.AuditLogMapper">

    <select id="selectRecentLogs" resultType="java.util.LinkedHashMap">
        SELECT id, created_at AS "createdAt", username, action, module,
               detail::text AS "detailJson", result
          FROM audit_log
         ORDER BY created_at DESC, id DESC
    </select>

    <insert id="insertReturningId" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_log(user_id, username, action, module, detail, result)
        VALUES(#{userId}, #{username}, #{action}, #{module},
               CAST(#{detail} AS jsonb), #{result})
    </insert>
</mapper>
