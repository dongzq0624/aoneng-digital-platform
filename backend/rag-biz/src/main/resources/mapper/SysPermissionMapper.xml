<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rag.mapper.SysPermissionMapper">

    <resultMap id="MenuOverview" type="java.util.LinkedHashMap">
        <id column="id" property="id"/>
        <result column="parentId" property="parentId"/>
        <result column="name" property="name"/>
        <result column="perms" property="perms"/>
        <result column="path" property="path"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="childCount" property="childCount"/>
        <result column="roleCount" property="roleCount"/>
    </resultMap>

    <select id="selectMenuOverviews" resultMap="MenuOverview">
        SELECT p.id, p.parent_id AS "parentId", p.name, p.perms, p.path, p.sort, p.status,
               (SELECT COUNT(*) FROM sys_permission c WHERE c.parent_id = p.id) AS "childCount",
               (SELECT COUNT(*) FROM sys_role_permission rp WHERE rp.permission_id = p.id) AS "roleCount"
          FROM sys_permission p
         WHERE p.type = 1
         ORDER BY p.sort, p.id
    </select>

    <insert id="insertReturningId" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_permission(parent_id, name, perms, type, path, sort, status)
        VALUES(#{parentId}, #{name}, #{perms}, #{type}, #{path}, #{sort}, #{status})
    </insert>

    <update id="updateMenu">
        UPDATE sys_permission
           SET parent_id = #{parentId},
               name      = COALESCE(#{name}, name),
               perms     = COALESCE(#{perms}, perms),
               path      = COALESCE(#{path}, path),
               sort      = COALESCE(#{sort}, sort),
               status    = COALESCE(#{status}, status)
         WHERE id = #{id}
    </update>

    <delete id="hardDelete">
        DELETE FROM sys_permission WHERE id = #{id}
    </delete>

    <select id="countMenuChildren" resultType="int">
        SELECT COUNT(*) FROM sys_permission WHERE parent_id = #{id}
    </select>

    <select id="countMenuRoles" resultType="int">
        SELECT COUNT(*) FROM sys_role_permission WHERE permission_id = #{id}
    </select>

    <select id="countMenuById" resultType="int">
        SELECT COUNT(*) FROM sys_permission WHERE id = #{id} AND type = 1
    </select>

    <select id="countByPerms" resultType="int">
        SELECT COUNT(*) FROM sys_permission WHERE perms = #{perms} AND id &lt;&gt; #{excludeId}
    </select>

    <select id="isMenuDescendant" resultType="int">
        WITH RECURSIVE tree AS (
            SELECT id FROM sys_permission WHERE id = #{ancestor}
            UNION ALL
            SELECT p.id FROM sys_permission p JOIN tree t ON p.parent_id = t.id
        )
        SELECT COUNT(*) FROM tree WHERE id = #{candidate}
    </select>

    <insert id="insertRolePermission">
        INSERT INTO sys_role_permission(role_id, permission_id) VALUES(#{roleId}, #{permissionId})
    </insert>
</mapper>
