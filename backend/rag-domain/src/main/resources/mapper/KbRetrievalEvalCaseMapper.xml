<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rag.domain.kb.mapper.KbRetrievalEvalCaseMapper">

    <select id="selectAllCases" resultType="java.util.LinkedHashMap">
        SELECT id, question,
               expected_chunk_ids AS "expectedChunkIdsRaw",
               enabled, note,
               created_at AS "createdAt", updated_at AS "updatedAt"
          FROM kb_retrieval_eval_case
         ORDER BY id
    </select>

    <select id="selectCaseById" resultType="java.util.LinkedHashMap">
        SELECT id, question,
               expected_chunk_ids AS "expectedChunkIdsRaw",
               enabled, note,
               created_at AS "createdAt", updated_at AS "updatedAt"
          FROM kb_retrieval_eval_case
         WHERE id = #{id}
    </select>

    <insert id="insertCase" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO kb_retrieval_eval_case(question, expected_chunk_ids, enabled, note)
        VALUES(#{question}, CAST(#{expectedChunkIds} AS bigint[]), #{enabled}, #{note})
    </insert>
</mapper>
