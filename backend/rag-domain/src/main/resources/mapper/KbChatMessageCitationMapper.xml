<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aoneng.rag.domain.chat.mapper.KbChatMessageCitationMapper">

    <insert id="insertCitation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO kb_chat_message_citation(message_id, chunk_id, doc_id, kb_id, file_name,
                                             page_no, snippet, rank_no, score)
        VALUES(#{messageId}, #{chunkId}, #{docId}, #{kbId}, #{fileName},
               #{pageNo}, #{snippet}, #{rankNo}, #{score})
    </insert>

    <select id="selectByMessage" resultType="com.aoneng.rag.domain.chat.po.KbChatMessageCitationPO">
        SELECT id, message_id AS "messageId", chunk_id AS "chunkId", doc_id AS "docId",
               kb_id AS "kbId", file_name AS "fileName", page_no AS "pageNo",
               snippet, rank_no AS "rankNo", score
          FROM kb_chat_message_citation
         WHERE message_id = #{messageId}
         ORDER BY rank_no
    </select>
</mapper>
